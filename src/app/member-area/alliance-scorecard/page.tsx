import { ScorecardWorkspace } from "@/components/CoachingPlatformViews";

export const metadata = { title: "Loan Factory Alliance Scorecard" };

export default function Page() {
  return <ScorecardWorkspace program="alliance" />;
}
