import { ProfileView } from "@/components/CoachingPlatformViews";

export const metadata = { title: "Loan Factory Alliance Profile" };

export default function Page() {
  return <ProfileView program="alliance" />;
}
