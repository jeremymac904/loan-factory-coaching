import { PlaybooksLibrary } from "@/components/CoachingPlatformViews";

export const metadata = { title: "Loan Factory Alliance Playbooks" };

export default function Page() {
  return <PlaybooksLibrary program="alliance" />;
}
